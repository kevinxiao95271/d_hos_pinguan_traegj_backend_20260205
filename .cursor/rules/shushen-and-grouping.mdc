---
description: 书审名单以 4.13 终稿为准；分组优先试算；评委吕娜须保留
alwaysApply: true
---

# 书审专家与分组（当前约定）

## 书审评委名单与组别

- **唯一依据**：根目录 **`2026年浙江省医院品管大赛书审专家名单-4.13.xlsx`**（表 **`书审终稿`**）。专家谁参加、分在哪一组，**以该文件为准**，不再以旧版 `书审专家单位(1)-0410.xlsx` 为准。
- 代码中 `trial_grouping_avoidance.resolve_shushen_excel()` 会**优先**匹配 `*书审*名单*4.13*.xlsx`。

## 分组与机构回避

- **优先采用试算结果**：`scripts/trial_grouping_avoidance.py` 输出的回避逻辑；定稿分组用 **`grouping_v4.xlsx`**（由 `scripts/export_grouping_v4.py` 基于 `grouping_v2` + 试算生成，并写 `scripts/grouping_v4_avoidance_report.txt` 校验冲突为 0）。
- 生产库报名 `group_code` 与专家任务分配，以试算/ v3 为准同步（由维护者执行）。

## 专家主数据与生产库

- 专家 **机构、账号字段** 对齐仍以 **`专家数据含机构ID0410.csv`** 为主数据；生产库刷新由维护者另行处理。
- **吕娜**（手机 `13588426741`，浙医二院）**须作为书审评委**：名单在 4.13 中、且须在 CSV/生产库中保留为评委账号；**不得**再将其列入「不参与/排除」名单。
