---
description: 专家与机构的对应关系以专家数据 CSV 为优先依据
alwaysApply: true
---

# 专家—机构数据来源优先级

- **主数据**：根目录下的 `专家数据含机构ID0410.csv`（列含 `institution_id`、`institution_name` 等）。
- **规则**：做专家归属、回避核对、导入脚本、与机构表关联时，**优先且以该文件为准**。
- **冲突处理（机构字段）**：若名单 Excel 与 CSV 在 **单位 / institution** 上不一致，**以 `专家数据含机构ID0410.csv` 为准**。
- **谁担任书审评委** 不以 CSV 为准：以 **`2026年浙江省医院品管大赛书审专家名单-4.13.xlsx`（书审终稿）** 为准（见 `shushen-and-grouping` 规则）。
