# 历史数据查询接口 - 交付说明

## ✅ 交付状态：已完成并测试通过

---

## 📊 接口信息

**接口地址**: `GET /api/historical-data`  
**服务地址**: http://localhost:6031  
**Swagger文档**: http://localhost:6031/swagger  
**数据总量**: 1818 条

---

## 🔐 权限

**可访问角色**:
- ✅ COMMITTEE_ADMIN（组委会管理员）
- ✅ OPS（系统运维）

**测试账号**: 
- 手机号: 13800000041
- 姓名: CommitteeAdmin A
- 角色: COMMITTEE_ADMIN

---

## 🎯 筛选功能（7个维度）

| 参数 | 说明 | 匹配方式 | 示例 |
|------|------|----------|------|
| year | 年份 | 精确 | 2024 |
| competitionGroup | 组别 | 精确 | 综合组 |
| region | 地区 | 模糊 | 杭州 |
| institutionName | 医院 | 模糊 | 人民医院 |
| projectName | 项目 | 模糊 | 提高 |
| circleName | 品管工具 | 模糊 | 肾利圈 |
| dataStatus | 状态 | 精确 | 已提交 |

**分页参数**:
- page: 页码（从0开始，默认0）
- size: 每页大小（默认20）

---

## 📈 数据统计

```
总记录数: 1818 条

按年份:
- 2024年: 826 条
- 2025年: 992 条

按组别:
- 综合组: 1249 条
- 基层组: 417 条
- 进阶组: 152 条

按地区:
- 杭州: 363 条
- 其他地区: 1455 条
```

---

## 💻 前端调用示例

### 1. 登录获取Token
```javascript
const response = await fetch('/api/auth/login', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    phone: '13800000041',
    name: 'CommitteeAdmin A',
    role: 'COMMITTEE_ADMIN'
  })
});
const { data } = await response.json();
const token = data.token;
```

### 2. 查询数据
```javascript
// 基本查询
const response = await fetch('/api/historical-data?page=0&size=20', {
  headers: { 'Authorization': `Bearer ${token}` }
});

// 按年份查询
const response = await fetch('/api/historical-data?year=2024&page=0&size=20', {
  headers: { 'Authorization': `Bearer ${token}` }
});

// 按组别查询
const response = await fetch('/api/historical-data?competitionGroup=综合组&page=0&size=20', {
  headers: { 'Authorization': `Bearer ${token}` }
});

// 组合查询
const params = new URLSearchParams({
  year: '2024',
  competitionGroup: '综合组',
  region: '杭州',
  page: 0,
  size: 20
});
const response = await fetch(`/api/historical-data?${params}`, {
  headers: { 'Authorization': `Bearer ${token}` }
});
```

### 3. 处理响应
```javascript
const result = await response.json();
if (result.success) {
  const { content, totalElements, totalPages, number } = result.data;
  console.log(`总记录数: ${totalElements}`);
  console.log(`总页数: ${totalPages}`);
  console.log(`当前页: ${number + 1}`);
  console.log(`数据:`, content);
}
```

---

## 🎨 组别选择器配置

```javascript
// 前端下拉框选项
const competitionGroups = [
  { label: '全部', value: '' },
  { label: '综合组', value: '综合组' },
  { label: '基层组', value: '基层组' },
  { label: '进阶组', value: '进阶组' }
];
```

**重要**: 组别必须使用以下精确值：
- `综合组`
- `基层组`
- `进阶组`

---

## ✅ 测试验证

| 测试项 | 结果 |
|--------|------|
| 全部数据查询 | ✅ 1818条 |
| 年份筛选（2024） | ✅ 826条 |
| 年份筛选（2025） | ✅ 992条 |
| 组别筛选（综合组） | ✅ 1249条 |
| 组别筛选（基层组） | ✅ 417条 |
| 组别筛选（进阶组） | ✅ 152条 |
| 地区筛选（杭州） | ✅ 363条 |
| 组合查询 | ✅ 正常 |
| 分页功能 | ✅ 正常 |
| 权限控制 | ✅ 正常 |

---

## 📚 相关文档

1. **API详细文档**: `docs/历史数据API文档.md`
2. **使用说明**: `docs/历史数据接口使用说明.md`
3. **测试报告**: `docs/最终测试报告.md`
4. **前端示例**: `docs/前端调用示例.html`（可直接打开测试）

---

## 🚀 快速测试

### 方法1: 使用测试脚本
```bash
python test_group_simple.py
```

### 方法2: 使用浏览器
1. 打开 `docs/前端调用示例.html`
2. 自动登录
3. 选择筛选条件
4. 点击"查询"按钮

### 方法3: 使用Swagger
访问 http://localhost:6031/swagger

---

## ⚠️ 注意事项

1. **必须先登录获取Token**
2. **Token放在请求头**: `Authorization: Bearer {token}`
3. **只有组委会管理员和系统运维可以访问**
4. **组别值必须精确匹配**: 综合组、基层组、进阶组
5. **建议使用分页**: 避免一次性加载过多数据

---

## 📞 技术支持

- **Swagger文档**: http://localhost:6031/swagger
- **健康检查**: http://localhost:6031/actuator/health
- **测试脚本**: `test_group_simple.py`

---

**交付日期**: 2026-02-08  
**开发状态**: ✅ 已完成  
**测试状态**: ✅ 已通过  
**可用状态**: ✅ 可正式使用
