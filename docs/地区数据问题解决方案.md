# 地区数据问题解决方案

## 🔍 问题发现

用户报告了三个关键问题：

1. ❌ 搜索"杭州市"返回 **0条数据**
2. 🤔 热门地区显示的是"义乌市"、"乐清市"、"上城区"、"萧山区"等
3. ⚠️ 地区列表有96个，都是区县级别，不是市级别

## 📊 数据分析结果

### 总体情况
- **总机构数**: 36,076 家
- **地区总数**: 96 个
  - 省级: 21 个
  - 市级: 21 个  
  - 区县级: **75 个**（主要）

### 杭州市详情
| 区县 | 机构数 | 占比 |
|------|--------|------|
| 上城区 | 1,091 | 3.02% |
| 萧山区 | 1,067 | 2.96% |
| 西湖区 | 888 | 2.46% |
| 滨江区 | 604 | 1.67% |
| 拱墅区 | 472 | 1.31% |
| 余杭区 | 439 | 1.22% |
| 富阳区 | 327 | 0.91% |
| 临安区 | 302 | 0.84% |
| 江干区 | 266 | 0.74% |
| 桐庐县 | 172 | 0.48% |
| 其他区县 | 158 | 0.44% |
| **合计** | **5,786** | **16.04%** |

### 热门城市分布
| 城市 | 机构数 | 占比 |
|------|--------|------|
| 杭州市 | 5,786 | 16.0% |
| 温州市 | 5,767 | 16.0% |
| 宁波市 | 4,563 | 12.6% |
| 金华市 | ~3,500 | ~9.7% |
| 其他7市 | ~16,460 | ~45.7% |

## 💡 解决方案：智能地区扩展

### 核心思路

**不修改数据库结构**，在代码层面实现智能地区匹配：

1. 保留原有 `region` 字段（区县级数据）
2. 新增 `RegionUtils` 工具类（城市->区县映射）
3. 搜索时自动扩展：市级 → 所有区县

### 实现细节

#### 1. 新增 `RegionUtils.java`

```java
package com.trae.pinguan.util;

public class RegionUtils {
    // 城市到区县的映射
    private static final Map<String, List<String>> CITY_TO_DISTRICTS = new HashMap<>();
    
    static {
        // 杭州市
        CITY_TO_DISTRICTS.put("杭州市", Arrays.asList(
            "上城区", "下城区", "江干区", "拱墅区", "西湖区", "滨江区",
            "萧山区", "余杭区", "富阳区", "临安区", "桐庐县", "淳安县", "建德市"
        ));
        // ... 其他10个城市
    }
    
    // 智能扩展地区查询
    public static List<String> expandRegionQuery(String regionInput) {
        if (regionInput == null) return Collections.emptyList();
        
        String input = regionInput.trim();
        
        // 1. 精确匹配市级
        if (CITY_TO_DISTRICTS.containsKey(input)) {
            return new ArrayList<>(CITY_TO_DISTRICTS.get(input));
        }
        
        // 2. 模糊匹配（如"杭州" -> "杭州市"）
        if (!input.endsWith("市") && !input.endsWith("区") && !input.endsWith("县")) {
            String cityKey = input + "市";
            if (CITY_TO_DISTRICTS.containsKey(cityKey)) {
                return new ArrayList<>(CITY_TO_DISTRICTS.get(cityKey));
            }
        }
        
        // 3. 不是市级，直接返回（可能是区县）
        return Collections.singletonList(input);
    }
}
```

#### 2. 修改 `InstitutionService.java`

```java
public Page<Institution> search(String keyword, String region, String level, ...) {
    // 智能扩展地区
    if (region != null && !region.trim().isEmpty()) {
        List<String> expandedRegions = RegionUtils.expandRegionQuery(region);
        
        // 如果扩展后有多个区县，使用动态查询
        if (expandedRegions.size() > 1) {
            return searchWithExpandedRegions(keyword, expandedRegions, level, pageable);
        }
    }
    // ... 原有查询逻辑
}

private Page<Institution> searchWithExpandedRegions(String keyword, List<String> regions, ...) {
    Specification<Institution> spec = (root, query, cb) -> {
        List<Predicate> predicates = new ArrayList<>();
        
        // 关键词条件
        if (keyword != null) {
            predicates.add(cb.or(
                cb.like(root.get("name"), "%" + keyword + "%"),
                cb.like(root.get("region"), "%" + keyword + "%")
            ));
        }
        
        // 地区条件（IN查询）
        if (!regions.isEmpty()) {
            predicates.add(root.get("region").in(regions));
        }
        
        // 等级条件
        if (level != null) {
            predicates.add(cb.equal(root.get("level"), level));
        }
        
        return cb.and(predicates.toArray(new Predicate[0]));
    };
    
    return institutionRepository.findAll(spec, pageable);
}
```

#### 3. 新增API接口

```java
@GetMapping("/cities")
@Operation(summary = "获取市级列表", description = "公开接口，返回11个地级市")
public ApiResponse<List<String>> getCities() {
    return ApiResponse.ok(RegionUtils.getAllCities());
}

@GetMapping("/districts")
@Operation(summary = "获取指定市的区县列表")
public ApiResponse<List<String>> getDistricts(@RequestParam String city) {
    return ApiResponse.ok(RegionUtils.getDistrictsByCity(city));
}
```

#### 4. 更新白名单

```java
private boolean isPublicInstitutionEndpoint(String path, String method) {
    if ("GET".equalsIgnoreCase(method)) {
        return path.equals("/api/institutions/cities")      // 新增
            || path.equals("/api/institutions/districts")   // 新增
            || path.equals("/api/institutions/regions")
            // ... 其他公开接口
    }
    // ...
}
```

---

## ✅ 测试验证

### 功能测试

```bash
python scripts/test_region_complete.py
```

**结果**:
- ✅ 搜索"杭州市" → **5,786家**（自动匹配13个区县）
- ✅ 搜索"杭州" → **5,786家**（智能识别）
- ✅ 搜索"上城区" → **1,091家**（精确匹配）
- ✅ 杭州市三级医疗 → **20家**
- ✅ 宁波市 → **4,563家**
- ✅ 温州市 → **5,767家**
- ✅ GET /api/institutions/cities → **11个城市**
- ✅ GET /api/institutions/districts?city=杭州市 → **13个区县**

### 性能测试

| 搜索范围 | 数据量 | 响应时间 |
|----------|--------|----------|
| 全局搜索 | 36,076 | ~200ms |
| 杭州市 | 5,786 | ~50ms |
| 上城区 | 1,091 | ~20ms |

---

## 🎨 前端集成建议

### 推荐方案：两级联动

```
[城市选择器] → [机构搜索框]
    ↓              ↓
 11个城市     在选定城市内搜索
```

**优势**:
1. 先缩小范围到城市（5千家内）
2. 搜索性能提升6倍+
3. 用户体验清晰

### API调用流程

```javascript
// Step 1: 加载城市列表
GET /api/institutions/cities
// 返回: ["杭州市", "宁波市", ...]

// Step 2: 用户选择"杭州市"

// Step 3: 在杭州市内搜索机构
POST /api/institutions/search
{
  "keyword": "人民医院",
  "region": "杭州市",  // 自动匹配13个区县
  "page": 0,
  "size": 10
}
// 返回: 5,786家中匹配"人民医院"的结果

// Step 4: 用户选择具体机构

// Step 5: 提交注册
POST /api/auth/register
{
  "institutionId": 123,
  // ... 其他字段
}
```

---

## 🔑 关键优势

### ✅ 技术优势

1. **无需改表**: 不修改数据库结构，保留原始数据
2. **向后兼容**: 原有区县搜索完全不受影响
3. **性能优化**: 通过 `IN` 查询 + 索引，性能优秀
4. **易于维护**: 映射关系在代码中，易于调整

### ✅ 用户体验

1. **直观**: 搜索"杭州市"就能找到所有杭州机构
2. **灵活**: 支持市级、区县级、关键词多种搜索方式
3. **准确**: 自动识别"杭州"、"杭州市"等变体
4. **高效**: 两级联动，快速定位目标机构

### ✅ 业务价值

1. **注册流程顺畅**: 用户可以方便地找到所在机构
2. **数据质量提升**: 规范了地区查询逻辑
3. **扩展性好**: 未来可轻松添加更多城市或修改映射

---

## 🚀 上线检查清单

- [x] RegionUtils.java 已创建
- [x] InstitutionService.java 已修改（智能扩展）
- [x] InstitutionRepository 已扩展（JpaSpecificationExecutor）
- [x] InstitutionController 已新增（/cities、/districts）
- [x] JwtAuthorizationFilter 已更新（白名单）
- [x] 编译通过
- [x] 服务运行
- [x] 功能测试通过
- [x] 性能测试通过
- [x] Swagger文档已更新
- [x] 前端集成文档已提供

---

## 📖 相关文档

- [前端集成指南-地区选择](./前端集成-地区选择.md)
- [Swagger接口清单](./Swagger接口清单.md)
- [机构搜索Token策略说明](./机构搜索Token策略说明.md)

---

**最后更新**: 2026-02-25  
**状态**: ✅ 已完成并验证
