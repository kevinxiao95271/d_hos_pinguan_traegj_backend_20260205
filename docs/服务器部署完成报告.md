# 服务器部署完成报告

## 部署信息

- **服务器IP**: 81.71.44.180
- **部署目录**: /data/pinguan
- **服务端口**: 6031
- **部署时间**: 2026-02-09 08:41

## 部署状态

✅ **部署成功！**

### 服务状态
- ✅ 进程正在运行（PID: 892566）
- ✅ 端口6031正在监听
- ✅ Spring Boot启动成功
- ✅ 数据库连接正常
- ✅ JPA初始化完成

### 启动日志
```
Started PinguanBackendApplication in 7.41 seconds
Tomcat started on port(s): 6031 (http)
```

## 访问地址

### 内网访问（服务器内部）
```bash
curl http://localhost:6031/actuator/health
curl http://localhost:6031/swagger
```

### 外网访问
```
http://81.71.44.180:6031/swagger
http://81.71.44.180:6031/actuator/health
```

**注意**: 如果外网无法访问，需要配置防火墙/安全组规则开放6031端口

## 服务管理命令

### 查看进程
```bash
ssh root@81.71.44.180
ps aux | grep pinguan-backend
```

### 查看日志
```bash
ssh root@81.71.44.180
tail -f /data/pinguan/app.log
```

### 停止服务
```bash
ssh root@81.71.44.180
pkill -f pinguan-backend
```

### 启动服务
```bash
ssh root@81.71.44.180
cd /data/pinguan
nohup java -jar pinguan-backend.jar > app.log 2>&1 &
```

### 重启服务
```bash
ssh root@81.71.44.180
pkill -f pinguan-backend
cd /data/pinguan
nohup java -jar pinguan-backend.jar > app.log 2>&1 &
```

## 部署文件

### 服务器文件列表
```
/data/pinguan/
├── pinguan-backend.jar    # 应用jar包
├── application.yml        # 配置文件
└── app.log               # 运行日志
```

### 本地部署脚本
- `scripts/deploy_to_server.py` - 自动部署脚本
- `scripts/check_server_status.py` - 状态检查脚本
- `scripts/view_server_log.py` - 日志查看脚本
- `deploy_manual_steps.txt` - 手动部署步骤

## 快速操作

### 重新部署
```bash
python scripts/deploy_to_server.py
```

### 检查状态
```bash
python scripts/check_server_status.py
```

### 查看日志
```bash
python scripts/view_server_log.py
```

## 数据库配置

服务使用的数据库配置（application.yml）：
- 主机: gz-cdb-bq7gk3k5.sql.tencentcdb.com
- 端口: 63606
- 数据库: d_hos_pinguan_traegj_20260205
- 用户: root

## 注意事项

1. **防火墙配置**: 如需外网访问，请在云服务器控制台配置安全组，开放6031端口
2. **日志管理**: 建议定期清理或轮转 app.log 文件
3. **服务监控**: 可配置进程守护工具（如systemd、supervisor）确保服务自动重启
4. **备份**: 定期备份jar包和配置文件

## 后续优化建议

1. 配置systemd服务，实现开机自启和自动重启
2. 配置nginx反向代理，提供HTTPS访问
3. 配置日志轮转（logrotate）
4. 配置监控告警（如Prometheus + Grafana）

---

**部署完成时间**: 2026-02-09 08:52
**部署状态**: ✅ 成功
