# 评委会筛选数据来源分析

**分析时间**: 2026-02-25 23:10

## 数据来源总结

评委会（后台管理）页面的下拉筛选框数据来源有**两种**：

### 1. 📁 Java 枚举类 (Enum)

存储在代码中，用于固定的、系统级的选项。

**位置**: `src/main/java/com/trae/pinguan/domain/enums/`

#### 枚举列表

| 枚举类 | 文件 | 用途 | 值 |
|--------|------|------|-----|
| **GroupType** | `GroupType.java` | 报名组别 | BASIC (基础组)<br>COMPREHENSIVE (综合组)<br>ADVANCED (高级组) |
| **RegistrationStatus** | `RegistrationStatus.java` | 报名状态 | DRAFT (草稿)<br>SUBMITTED (已提交)<br>RETURNED (已退回)<br>APPROVED (已通过) |
| **ReviewStage** | `ReviewStage.java` | 评审阶段 | BOOK (书面评审)<br>INTERVIEW (面谈评审)<br>FINAL (决赛评审) |
| **ReviewStatus** | `ReviewStatus.java` | 评审任务状态 | PENDING (待评审)<br>CONFIRMED (已确认)<br>SCORED (已评分)<br>RETURNED (已退回) |
| **CompetitionStage** | `CompetitionStage.java` | 赛事阶段 | REGISTER (报名阶段)<br>BOOK_REVIEW (书面评审)<br>INTERVIEW (面谈)<br>FINAL (决赛) |
| **RoleType** | `RoleType.java` | 用户角色 | CONTESTANT (参赛者)<br>REVIEWER (评委)<br>COMMITTEE_ADMIN (组委会管理员)<br>OPS (运营) |
| **MemberRole** | `MemberRole.java` | 成员角色 | LEADER (圈长)<br>MENTOR (辅导员)<br>PARTICIPANT (圈员) |
| **ApprovalStatus** | `ApprovalStatus.java` | 审核状态 | (具体值未查看) |

---

### 2. 🗄️ 数据库字典表 (dictionary_items)

存储在数据库中，用于灵活可变的业务数据。

**位置**: 数据库表 `dictionary_items`

#### 字典类型列表

| 类型代码 (type) | 名称 | 数量 | API接口 | 用途 |
|----------------|------|------|---------|------|
| **subject_type** | 主题类型 | 9条 | `GET /api/dictionaries/subject_type` | 报名活动的主题类型筛选 |
| **method** | 运用手法 | 10条 | `GET /api/dictionaries/method` | 报名活动的运用手法筛选 |
| **experience_improve** | 改善就医感受 | 12条 | `GET /api/dictionaries/experience_improve` | 报名活动的改善类型筛选 |
| **quality_topic** | 医疗质量安全主题 | 15条 | `GET /api/dictionaries/quality_topic` | 报名活动的质量主题筛选 |

**数据示例**:
```sql
SELECT * FROM dictionary_items WHERE type = 'method' LIMIT 3;

-- 结果:
-- id | type   | code | label           | active | created_at
-- 10 | method | qcc  | 品管圈(QCC)      | 1      | 2026-02-25 23:00:00
-- 11 | method | pdca | PDCA循环         | 1      | 2026-02-25 23:00:00
-- 12 | method | lean | 精益管理         | 1      | 2026-02-25 23:00:00
```

---

## 各页面使用的筛选数据

### 📋 后台报名管理页面

**接口**: `GET /api/admin/registrations/filter`

**筛选参数**:

| 参数 | 数据来源 | 说明 |
|------|---------|------|
| `competitionId` | competitions 表 | 赛事ID |
| `groupType` | **枚举 GroupType** | 组别 (BASIC/COMPREHENSIVE/ADVANCED) |
| `groupCode` | registrations.group_code | 分组代码 (动态生成，如 "A组"、"B组") |
| `projectName` | registrations.project_name | 项目名称 (文本搜索) |
| `institutionName` | institutions.name | 机构名称 (文本搜索) |
| `methodCode` | **字典表 method** | 运用手法 |
| `subjectTypeCode` | **字典表 subject_type** | 主题类型 |

**Controller**: `AdminRegistrationController.filter()`

---

### 👥 后台评委管理页面

**接口**: `GET /api/admin/reviewers`

**筛选参数**:

| 参数 | 数据来源 | 说明 |
|------|---------|------|
| `competitionId` | competitions 表 | 赛事ID (未实际使用) |
| `institutionId` | user_accounts.institution_id | 评委所属机构 |
| `reviewerGroupCode` | user_accounts.reviewer_group_code | 评委组代码 (如 "GROUP_A") |
| `interviewGroupCode` | user_accounts.interview_group_code | 面试组代码 (如 "INTERVIEW_1") |
| `expertBackground` | user_accounts.expert_background | 专家背景 (如 "心内科") |

**数据特点**:
- 这些都是 **user_accounts 表的字段**
- 不是枚举，也不是字典表
- 值是在创建评委时手动填写的
- 可以通过查询去重获取可用选项

**Controller**: `AdminReviewerController.list()`

---

### 📊 后台评审任务页面

**接口**: `GET /api/admin/review/tasks/filter`

**筛选参数**:

| 参数 | 数据来源 | 说明 |
|------|---------|------|
| `reviewerId` | user_accounts.id | 评委ID |
| `stage` | **枚举 ReviewStage** | 评审阶段 (BOOK/INTERVIEW/FINAL) |
| `status` | **枚举 ReviewStatus** | 任务状态 (PENDING/CONFIRMED/SCORED/RETURNED) |

**Controller**: `ReviewController.listWithFilter()`

---

## 数据获取方式对比

### 枚举类数据

**优点**:
- ✅ 固定值，不会变化
- ✅ 类型安全，编译时检查
- ✅ 性能好，无需查询数据库

**获取方式**:
```javascript
// 前端需要硬编码或从API schema获取
const groupTypes = [
  { value: 'BASIC', label: '基础组' },
  { value: 'COMPREHENSIVE', label: '综合组' },
  { value: 'ADVANCED', label: '高级组' }
];

// 或者从Swagger文档的enum定义获取
```

**后端提供枚举值的API** (如果需要):
```java
@GetMapping("/enums/group-types")
public ApiResponse<Map<String, String>> getGroupTypes() {
    Map<String, String> map = new LinkedHashMap<>();
    map.put("BASIC", "基础组");
    map.put("COMPREHENSIVE", "综合组");
    map.put("ADVANCED", "高级组");
    return ApiResponse.ok(map);
}
```

---

### 字典表数据

**优点**:
- ✅ 灵活可变，无需修改代码
- ✅ 支持动态增删改
- ✅ 有管理界面

**获取方式**:
```javascript
// 从API获取
const response = await axios.get('/api/dictionaries/method');
const methods = response.data.data;
// [
//   { id: 10, code: 'qcc', label: '品管圈(QCC)', active: true },
//   { id: 11, code: 'pdca', label: 'PDCA循环', active: true },
//   ...
// ]
```

---

### user_accounts 表字段数据

**特点**:
- 不是固定选项
- 值是在创建记录时手动填写的
- 需要查询去重获取可用选项

**如需获取可用选项**，需要添加API:
```java
@GetMapping("/reviewers/distinct-fields")
public ApiResponse<Map<String, List<String>>> getDistinctFields() {
    Map<String, List<String>> result = new HashMap<>();
    
    // 获取所有不重复的评委组代码
    result.put("reviewerGroupCodes", 
        userAccountRepository.findDistinctReviewerGroupCodes());
    
    // 获取所有不重复的面试组代码
    result.put("interviewGroupCodes", 
        userAccountRepository.findDistinctInterviewGroupCodes());
    
    // 获取所有不重复的专家背景
    result.put("expertBackgrounds", 
        userAccountRepository.findDistinctExpertBackgrounds());
    
    return ApiResponse.ok(result);
}
```

---

## 两种数据存储方式的使用场景

### 使用枚举类 (Enum) 的情况

✅ **适用于**:
- 系统级别的固定选项
- 值在系统生命周期内不变
- 需要在代码中进行类型检查
- 数量少且稳定

**示例**:
- 用户角色 (CONTESTANT, REVIEWER, ADMIN)
- 报名状态 (DRAFT, SUBMITTED, APPROVED)
- 评审阶段 (BOOK, INTERVIEW, FINAL)

### 使用字典表 (dictionary_items) 的情况

✅ **适用于**:
- 业务级别的可变选项
- 值可能随业务需求变化
- 需要提供管理界面增删改
- 数量多或经常变化

**示例**:
- 主题类型 (临床护理、医疗技术...)
- 运用手法 (品管圈、PDCA...)
- 改善类型 (预约诊疗、等候时间...)

---

## 总结

### 当前系统的数据分布

| 数据类型 | 存储位置 | 数量 | 是否可变 | 获取方式 |
|---------|---------|------|---------|---------|
| **系统枚举** | Java代码 | 8个枚举类 | ❌ 固定 | 前端硬编码或API |
| **业务字典** | dictionary_items表 | 4个类型46条 | ✅ 可变 | GET /api/dictionaries/{type} |
| **动态字段** | user_accounts等表 | N/A | ✅ 动态 | 需要添加去重查询API |

### 建议

1. **枚举类数据**: 前端可以硬编码，或者后端提供一个统一的枚举API
2. **字典表数据**: 已有API，继续使用
3. **动态字段数据**: 如需筛选，建议添加去重查询API

---

**📝 备注**: 本文档仅作数据来源分析，不涉及任何代码或数据的修改。
