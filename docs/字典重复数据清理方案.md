# 字典重复数据清理方案

**生成时间**: 2026-02-27  
**问题**: 主题类型、运用手法、改善就医感受、医疗质量安全主题 四个字典类型存在重复数据  
**影响**: 前端下拉列表显示两套重复数据

---

## 📊 问题统计

| 字典类型 | 总数 | 唯一label | 重复label | 需要删除 |
|---------|------|----------|----------|---------|
| 主题类型 | 21条 | 11个 | **10个** | 10条 |
| 运用手法 | 33条 | 19个 | **14个** | 14条 |
| 改善就医感受 | 13条 | 8个 | **5个** | 5条 |
| 医疗质量安全主题 | 26条 | 17个 | **9个** | 9条 |
| **合计** | **93条** | **55个** | **38个** | **38条** |

**清理后**: 93条 → 55条（减少 38 条，减少 40.9%）

---

## 🎯 清理策略

### 决策规则

对于每个重复的 label：
1. **保留使用次数多的 code**
2. 如果使用次数相同，保留**语义化更好的 code**（如 `patient_care` 优于 `subject_type_1`）
3. 未使用的直接删除，已使用的先迁移数据再删除

---

## 📋 详细清理列表

### 1. 主题类型（subject_type）- 需要清理 10 条

| Label | 保留Code | 使用次数 | 删除Code | 使用次数 | 操作 |
|-------|---------|---------|---------|---------|------|
| 患者护理 | `patient_care` | 12 | `subject_type_1` | 5 | 迁移5条 |
| 病例质量 | `subject_type_2` | 6 | `case_quality` | 2 | 迁移2条 |
| 时间效率 | `time_efficiency` | 7 | `subject_type_3` | 6 | 迁移6条 |
| 成本效益 | `cost_efficiency` | 8 | `subject_type_4` | 2 | 迁移2条 |
| 安全环境 | `safety_env` | 2 | `subject_type_5` | 1 | 迁移1条 |
| 满意度 | `subject_type_6` | 8 | `satisfaction` | 4 | 迁移4条 |
| 教育培训 | `education` | 8 | `subject_type_7` | 3 | 迁移3条 |
| 医疗信息 | `subject_type_8` | 8 | `info` | 5 | 迁移5条 |
| 医疗质量与安全 | `quality_safety` | 6 | `subject_type_9` | 5 | 迁移5条 |
| 流程改进 | `process` | 9 | `subject_type_10` | 3 | 迁移3条 |

**小计**: 需要迁移 **36 条记录**

---

### 2. 运用手法（method）- 需要清理 14 条

| Label | 保留Code | 使用次数 | 删除Code | 使用次数 | 操作 |
|-------|---------|---------|---------|---------|------|
| 5S | `5s` | 4 | `method_8` | 2 | 迁移2条 |
| PDCA | `method_13` | 5 | `pdca` | 3 | 迁移3条 |
| QFD | `method_9` | 4 | `qfd` | 0 | **直接删除** ✅ |
| TRM | `trm` | 3 | `method_14` | 3 | 迁移3条 |
| 专题改进 | `method_3` | 4 | `project_improve` | 3 | 迁移3条 |
| 六西格玛管理 | `method_11` | 6 | `six_sigma` | 2 | 迁移2条 |
| 品管圈-课题 | `qc_topic` | 7 | `method_2` | 3 | 迁移3条 |
| 品管圈-问题 | `method_1` | 4 | `qc_problem` | 3 | 迁移3条 |
| 品质报告案 | `method_10` | 9 | `quality_report` | 4 | 迁移4条 |
| 失效模式与效应分析 | `method_6` | 2 | `fmea` | 1 | 迁移1条 |
| 平衡计分卡 | `method_4` | 4 | `balanced_scorecard` | 3 | 迁移3条 |
| 循证医学 | `ebm` | 3 | `method_12` | 2 | 迁移2条 |
| 根本原因分析 | `method_5` | 6 | `root_cause` | 0 | **直接删除** ✅ |
| 流程改进 | `method_15` | 6 | `process_improve` | 0 | **直接删除** ✅ |

**小计**: 需要迁移 **33 条记录**，直接删除 **3 条字典项**

---

### 3. 改善就医感受（experience_improve）- 需要清理 5 条

| Label | 保留Code | 使用次数 | 删除Code | 使用次数 | 操作 |
|-------|---------|---------|---------|---------|------|
| 预约诊疗服务更加便捷 | `appointment` | 8 | `appointment_service` | 8 | 迁移8条 |
| 舒心就医环境更加温馨 | `environment` | 10 | `comfortable_environment` | 10 | 迁移10条 |
| 院前院内衔接更加高效 | `pre_inpatient_connection` | 11 | `pre_in_out` | 10 | 迁移10条 |
| 院后医疗服务更加连续 | `post_hospital_service` | 10 | `post_discharge` | 6 | 迁移6条 |
| 互联网诊疗更加可及 | `internet_med` | 8 | `internet_diagnosis` | 6 | 迁移6条 |

**小计**: 需要迁移 **40 条记录**

---

### 4. 医疗质量安全主题（quality_topic）- 需要清理 9 条

| Label | 保留Code | 使用次数 | 删除Code | 使用次数 | 操作 |
|-------|---------|---------|---------|---------|------|
| 急性心梗ST段抬高... | `stemi` | 4 | `stemi_reperfusion` | 3 | 迁移3条 |
| 急性脑梗再灌注... | `stroke` | 5 | `stroke_reperfusion` | 5 | 迁移5条 |
| 恶性肿瘤术前癌... | `tumor_tnm_staging` | 5 | `tnm` | 3 | 迁移3条 |
| 降低静脉血栓栓塞症... | `vte_prevention` | 6 | `vte` | 3 | 迁移3条 |
| 降低感染性休克... | `sepsis_bundle` | 5 | `septic_shock_bundle` | 3 | 迁移3条 |
| 降低围手术期死亡率 | `perioperative_mortality` | 7 | `surgery_mortality` | 4 | 迁移4条 |
| 降低关键诊疗行为... | `key_diagnosis_record` | 5 | `record_integrity` | 3 | 迁移3条 |
| 降低医疗安全不良... | `adverse_event_report` | 5 | `event_reporting` | 2 | 迁移2条 |
| 四级手术术前多学科... | `mdt` | 3 | `level4_surgery_mdt` | 2 | 迁移2条 |

**小计**: 需要迁移 **28 条记录**

---

## 📊 清理统计汇总

### 数据迁移

| 字典类型 | 需要迁移的字典项 | 需要更新的记录数 |
|---------|----------------|----------------|
| 主题类型 | 10个 | 36条 |
| 运用手法 | 11个 | 33条 |
| 改善就医感受 | 5个 | 40条 |
| 医疗质量安全主题 | 9个 | 28条 |
| **合计** | **35个** | **137条** |

### 直接删除

| 字典类型 | 可直接删除 |
|---------|-----------|
| 运用手法 | 3个 (`qfd`, `root_cause`, `process_improve`) |
| **合计** | **3个** |

### 总计

- **删除字典项**: 38 条（35个需要先迁移 + 3个直接删除）
- **更新数据记录**: 137 条
- **清理后字典项数**: 93条 → 55条

---

## ⚠️ 风险评估

### 低风险 ✅

1. **数据完整性**: 所有迁移都是同义替换，label 不变
2. **功能影响**: 前端显示的 label 完全一致，用户无感知
3. **可回滚**: 可以记录迁移前的数据，必要时回滚

### 注意事项

1. **前端 code 依赖**: 
   - 如果前端代码中硬编码了某些 code（如 `if (code === 'subject_type_1')`）
   - 需要同步更新前端代码
   - 但通常前端只显示 label，不依赖 code

2. **API 兼容性**:
   - API 返回的 code 会变化
   - 但 label 不变，对用户透明

---

## 🔧 执行计划

### Step 1: 数据备份（可选）

```sql
-- 备份 activity_infos 表（可选）
CREATE TABLE activity_infos_backup_20260227 AS SELECT * FROM activity_infos;
```

### Step 2: 数据迁移（35 个字典项，137 条记录）

**执行方式**: 批量执行 SQL（已自动生成）

**预计耗时**: 1-2 秒

**影响**: 137 条 `activity_infos` 记录的 code 字段会被更新

### Step 3: 删除字典项（38 条）

**执行方式**: 单条 SQL 删除

```sql
DELETE FROM dictionary_items WHERE id IN (78, 74, 84, 67, ...);
```

**预计耗时**: < 1 秒

### Step 4: 验证

**检查项**:
1. ✅ 字典表中每个 label 只有一条记录
2. ✅ 所有 activity_infos 的 code 都能在字典表中找到
3. ✅ API 返回的下拉列表无重复

---

## 📝 预期效果

### 清理前 ❌

**API 返回** (`GET /api/dictionaries/subject_type`):
```json
[
  {"code": "patient_care", "label": "患者护理"},
  {"code": "subject_type_1", "label": "患者护理"},  ← 重复
  {"code": "cost_efficiency", "label": "成本效益"},
  {"code": "subject_type_4", "label": "成本效益"},  ← 重复
  ...
]
```

**前端下拉框**: 显示两个"患者护理"，用户困惑

---

### 清理后 ✅

**API 返回**:
```json
[
  {"code": "patient_care", "label": "患者护理"},
  {"code": "cost_efficiency", "label": "成本效益"},
  ...
]
```

**前端下拉框**: 每个 label 只显示一次，清晰明了

---

## 🎯 保留规则说明

### 为什么选择这些 code？

我的决策规则是**优先保留使用次数多的**，具体如下：

#### 示例1: 患者护理
- `patient_care`: 12次使用 ✅ **保留**（语义化好 + 使用多）
- `subject_type_1`: 5次使用 ❌ 删除（通用命名 + 使用少）

#### 示例2: 医疗信息
- `subject_type_8`: 8次使用 ✅ **保留**（使用更多）
- `info`: 5次使用 ❌ 删除（虽然语义化，但使用少）

#### 示例3: PDCA
- `method_13`: 5次使用 ✅ **保留**（使用更多）
- `pdca`: 3次使用 ❌ 删除（虽然语义化，但使用少）

### 规则权重

1. **使用次数** > 语义化程度
2. 尽量减少数据迁移量
3. 保持系统稳定性

---

## 💡 特殊情况说明

### 情况1: 使用次数相同

**示例**: "预约诊疗服务更加便捷"
- `appointment`: 8次
- `appointment_service`: 8次

**决策**: 保留 `appointment`（更简洁）

---

### 情况2: 一个新一个旧，使用次数接近

**示例**: "院前院内衔接更加高效"
- `pre_inpatient_connection`: 11次 ✅ 保留（语义化 + 使用稍多）
- `pre_in_out`: 10次 ❌ 删除

**理由**: 新 code 更清晰，且使用更多

---

## 🔍 代码影响分析

### 后端代码

**无需修改**: 
- ✅ 后端使用 `getLabel(code)` 方法查询
- ✅ code 变化后，label 保持不变
- ✅ 业务逻辑完全不受影响

### 前端代码

**需要检查**:
1. 前端是否硬编码了 code？
   ```javascript
   // ❌ 如果有这样的代码
   if (record.subjectTypeCode === 'subject_type_1') { ... }
   ```
2. 如果有，需要同步更新前端

**通常情况**: 
- ✅ 前端只显示 label
- ✅ code 用于提交数据，但不做判断
- ✅ 不需要改前端

---

## ✅ 执行检查清单

### 执行前

- [ ] 确认前端代码中没有硬编码 code（或已同步更新）
- [ ] 确认测试环境可以正常访问
- [ ] （可选）备份 activity_infos 表

### 执行中

- [ ] Step 1: 执行数据迁移（137条记录）
- [ ] Step 2: 删除重复字典项（38条）
- [ ] Step 3: 验证清理结果

### 执行后

- [ ] 验证字典表无重复 label
- [ ] 验证 API 下拉列表无重复
- [ ] 验证前端功能正常
- [ ] 测试报名详情显示正常

---

## 📜 完整 SQL 脚本

**已自动生成**，位于脚本输出中，包含：

1. **35 条 UPDATE 语句**（数据迁移）
2. **1 条 DELETE 语句**（删除 38 个字典项）
3. **4 条验证 SQL**（检查是否还有重复）

**执行顺序**: 必须按顺序执行，不能颠倒

---

## 🎯 下一步

**现在需要您确认**:

1. ✅ **是否同意这个清理方案？**
   - 保留使用次数多的 code
   - 删除使用次数少的 code
   - 迁移 137 条记录

2. ✅ **是否需要检查前端代码？**
   - 确认前端没有硬编码 code

3. ✅ **确认后我将执行**:
   - 创建执行脚本
   - 执行数据迁移
   - 删除重复字典项
   - 验证清理结果
   - 测试 API

---

**请您确认是否执行清理操作？** 🚀
