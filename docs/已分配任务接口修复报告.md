# 已分配任务接口修复报告

## 📋 问题描述

**用户反馈**: 书审阶段和面谈阶段的已分配任务列表中，除了序号外，其他字段（项目名称、医疗机构、组别、分组、评委姓名、职称、评委机构、状态）都没有值。

## 🔍 问题原因

### 1. 缺少管理端查询接口

原有的 `AdminReviewController` 只有以下接口：
- `POST /api/admin/reviews/tasks` - 分配任务
- `POST /api/admin/reviews/auto-assign` - 自动分配任务
- `GET /api/admin/reviews/summary` - 评分汇总
- `GET /api/admin/reviews/rankings` - 评分排名

**缺少查询已分配任务列表的接口！**

### 2. ReviewTask 实体的序列化问题

`ReviewTask` 实体中的关联字段使用了 `@JsonIgnore` 注解：

```java
@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "registration_id")
@JsonIgnore  // ← 这导致 registration 字段不会被序列化
private Registration registration;

@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "reviewer_id")
@JsonIgnore  // ← 这导致 reviewer 字段不会被序列化
private UserAccount reviewer;
```

即使有接口返回 `ReviewTask` 实体，前端也无法获取到报名信息和评委信息。

### 3. 前端可能使用了错误的接口

前端可能使用了以下接口之一：
- `GET /api/reviews/tasks?reviewerId={id}` - 评委端接口，需要 reviewerId 参数
- `GET /api/reviews/tasks/stage?competitionId={id}&stage={stage}` - 返回 `ReviewTask` 实体，字段被 `@JsonIgnore` 隐藏

这些接口都不适合管理端使用。

## ✅ 解决方案

### 1. 创建新的 DTO

创建 `AdminReviewTaskItem` DTO，包含所有需要的字段：

```java
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class AdminReviewTaskItem {
    // 任务信息
    private Long id;
    private ReviewStage stage;
    private ReviewStatus status;
    private LocalDateTime createdAt;
    
    // 报名信息
    private Long registrationId;
    private String projectName;
    private String institutionName;
    private GroupType groupType;
    private String groupCode;
    
    // 评委信息
    private Long reviewerId;
    private String reviewerName;
    private String reviewerTitle;
    private String reviewerInstitutionName;
    private String reviewerGroupCode;
    private String interviewGroupCode;
    private String expertBackground;
}
```

### 2. 在 ReviewService 中添加新方法

```java
@Transactional(readOnly = true)
public List<AdminReviewTaskItem> listTasksForAdmin(Long competitionId, ReviewStage stage, ReviewStatus status) {
    List<ReviewTask> tasks;
    if (status == null) {
        tasks = reviewTaskRepository.findByStageAndRegistrationCompetitionId(stage, competitionId);
    } else {
        tasks = reviewTaskRepository.findByStageAndStatusAndRegistrationCompetitionId(stage, status, competitionId);
    }
    
    return tasks.stream()
            .map(task -> {
                Registration reg = task.getRegistration();
                UserAccount reviewer = task.getReviewer();
                
                return AdminReviewTaskItem.builder()
                        .id(task.getId())
                        .stage(task.getStage())
                        .status(task.getStatus())
                        .createdAt(task.getCreatedAt())
                        // 报名信息
                        .registrationId(reg != null ? reg.getId() : null)
                        .projectName(reg != null ? reg.getProjectName() : null)
                        .institutionName(reg != null && reg.getInstitution() != null 
                                ? reg.getInstitution().getName() : null)
                        .groupType(reg != null ? reg.getGroupType() : null)
                        .groupCode(reg != null ? reg.getGroupCode() : null)
                        // 评委信息
                        .reviewerId(reviewer != null ? reviewer.getId() : null)
                        .reviewerName(reviewer != null ? reviewer.getName() : null)
                        .reviewerTitle(reviewer != null ? reviewer.getTitle() : null)
                        .reviewerInstitutionName(reviewer != null && reviewer.getInstitution() != null 
                                ? reviewer.getInstitution().getName() : null)
                        .reviewerGroupCode(reviewer != null ? reviewer.getReviewerGroupCode() : null)
                        .interviewGroupCode(reviewer != null ? reviewer.getInterviewGroupCode() : null)
                        .expertBackground(reviewer != null ? reviewer.getExpertBackground() : null)
                        .build();
            })
            .collect(Collectors.toList());
}
```

### 3. 在 AdminReviewController 中添加新接口

```java
@GetMapping("/tasks")
@Operation(summary = "后台查询已分配任务列表")
public ApiResponse<List<AdminReviewTaskItem>> listTasks(
        @RequestParam Long competitionId,
        @RequestParam ReviewStage stage,
        @RequestParam(required = false) ReviewStatus status) {
    return ApiResponse.ok(reviewService.listTasksForAdmin(competitionId, stage, status));
}
```

## 📝 新接口说明

### 接口定义

```
GET /api/admin/reviews/tasks
```

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| competitionId | Long | 是 | 赛事ID |
| stage | ReviewStage | 是 | 评审阶段（BOOK/INTERVIEW/FINAL） |
| status | ReviewStatus | 否 | 任务状态（PENDING/SCORED/RETURNED） |

### 返回数据

```json
{
  "success": true,
  "data": [
    {
      "id": 115,
      "stage": "BOOK",
      "status": "SCORED",
      "createdAt": "2026-02-06T18:12:37.447",
      "registrationId": 106,
      "projectName": "护理交接班规范化-1",
      "institutionName": "浙江大学医学院附属第二医院（浙二医院）",
      "groupType": "BASIC",
      "groupCode": "A1",
      "reviewerId": 6,
      "reviewerName": "李明华",
      "reviewerTitle": "Test Title",
      "reviewerInstitutionName": "浙江大学医学院附属第二医院（浙二医院）",
      "reviewerGroupCode": "A1",
      "interviewGroupCode": "A1",
      "expertBackground": "MEDICAL"
    }
  ]
}
```

### 字段说明

#### 任务信息
- `id`: 任务ID
- `stage`: 评审阶段（BOOK=书审, INTERVIEW=面谈, FINAL=决赛）
- `status`: 任务状态（PENDING=待评审, SCORED=已评分, RETURNED=已退回）
- `createdAt`: 创建时间

#### 报名信息
- `registrationId`: 报名ID
- `projectName`: 项目名称
- `institutionName`: 医疗机构名称
- `groupType`: 组别（BASIC=基层组, COMPREHENSIVE=综合组, ADVANCED=进阶组）
- `groupCode`: 分组代码（A1, A2, B1, B2等）

#### 评委信息
- `reviewerId`: 评委ID
- `reviewerName`: 评委姓名
- `reviewerTitle`: 评委职称
- `reviewerInstitutionName`: 评委所在机构
- `reviewerGroupCode`: 书审分组代码
- `interviewGroupCode`: 面谈分组代码
- `expertBackground`: 专家背景（MEDICAL=医疗, NURSING=护理, MANAGEMENT=管理）

## 🧪 测试结果

### 测试1: 查询书审阶段所有任务

**请求**:
```
GET /api/admin/reviews/tasks?competitionId=21&stage=BOOK
```

**结果**: ✅ 成功
- 返回6条任务
- 所有字段都有值
- 字段完整性100%

### 测试2: 查询面谈阶段所有任务

**请求**:
```
GET /api/admin/reviews/tasks?competitionId=21&stage=INTERVIEW
```

**结果**: ✅ 成功
- 返回1条任务
- 所有字段都有值

### 测试3: 按状态筛选

**请求**:
```
GET /api/admin/reviews/tasks?competitionId=21&stage=BOOK&status=PENDING
```

**结果**: ✅ 成功
- 返回5条待评审任务

**请求**:
```
GET /api/admin/reviews/tasks?competitionId=21&stage=BOOK&status=SCORED
```

**结果**: ✅ 成功
- 返回1条已评分任务

### 字段完整性检查

| 字段 | 缺失数量 | 完整性 |
|------|----------|--------|
| projectName | 0 | ✅ 100% |
| institutionName | 0 | ✅ 100% |
| groupType | 0 | ✅ 100% |
| groupCode | 0 | ✅ 100% |
| reviewerName | 0 | ✅ 100% |
| reviewerTitle | 0 | ✅ 100% |
| reviewerInstitutionName | 0 | ✅ 100% |

## 📚 前端调用示例

### Vue 3 示例

```vue
<template>
  <div>
    <el-table :data="tasks" border>
      <el-table-column type="index" label="序号" width="60" />
      <el-table-column prop="projectName" label="项目名称" />
      <el-table-column prop="institutionName" label="医疗机构" />
      <el-table-column prop="groupType" label="组别">
        <template #default="{ row }">
          {{ groupTypeLabel(row.groupType) }}
        </template>
      </el-table-column>
      <el-table-column prop="groupCode" label="分组" />
      <el-table-column prop="reviewerName" label="评委姓名" />
      <el-table-column prop="reviewerTitle" label="职称" />
      <el-table-column prop="reviewerInstitutionName" label="评委机构" />
      <el-table-column prop="status" label="状态">
        <template #default="{ row }">
          {{ statusLabel(row.status) }}
        </template>
      </el-table-column>
    </el-table>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue';
import { useAuthStore } from '@/stores/auth';

const authStore = useAuthStore();
const tasks = ref([]);

const loadTasks = async (competitionId, stage) => {
  try {
    const response = await fetch(
      `/api/admin/reviews/tasks?competitionId=${competitionId}&stage=${stage}`,
      {
        headers: {
          'Authorization': `Bearer ${authStore.token}`
        }
      }
    );
    
    const result = await response.json();
    
    if (result.success) {
      tasks.value = result.data;
    } else {
      console.error('获取任务列表失败:', result.message);
    }
  } catch (error) {
    console.error('请求失败:', error);
  }
};

const groupTypeLabel = (type) => {
  const labels = {
    'BASIC': '基层组',
    'COMPREHENSIVE': '综合组',
    'ADVANCED': '进阶组'
  };
  return labels[type] || type;
};

const statusLabel = (status) => {
  const labels = {
    'PENDING': '待评审',
    'SCORED': '已评分',
    'RETURNED': '已退回'
  };
  return labels[status] || status;
};

onMounted(() => {
  // 加载书审阶段的任务
  loadTasks(21, 'BOOK');
});
</script>
```

### React 示例

```jsx
import { useState, useEffect } from 'react';

function ReviewTaskList({ competitionId, stage }) {
  const [tasks, setTasks] = useState([]);
  const [loading, setLoading] = useState(false);
  
  useEffect(() => {
    loadTasks();
  }, [competitionId, stage]);
  
  const loadTasks = async () => {
    setLoading(true);
    
    try {
      const token = localStorage.getItem('token');
      const response = await fetch(
        `/api/admin/reviews/tasks?competitionId=${competitionId}&stage=${stage}`,
        {
          headers: {
            'Authorization': `Bearer ${token}`
          }
        }
      );
      
      const result = await response.json();
      
      if (result.success) {
        setTasks(result.data);
      } else {
        console.error('获取任务列表失败:', result.message);
      }
    } catch (error) {
      console.error('请求失败:', error);
    } finally {
      setLoading(false);
    }
  };
  
  const groupTypeLabel = (type) => {
    const labels = {
      'BASIC': '基层组',
      'COMPREHENSIVE': '综合组',
      'ADVANCED': '进阶组'
    };
    return labels[type] || type;
  };
  
  const statusLabel = (status) => {
    const labels = {
      'PENDING': '待评审',
      'SCORED': '已评分',
      'RETURNED': '已退回'
    };
    return labels[status] || status;
  };
  
  if (loading) return <div>加载中...</div>;
  
  return (
    <table>
      <thead>
        <tr>
          <th>序号</th>
          <th>项目名称</th>
          <th>医疗机构</th>
          <th>组别</th>
          <th>分组</th>
          <th>评委姓名</th>
          <th>职称</th>
          <th>评委机构</th>
          <th>状态</th>
        </tr>
      </thead>
      <tbody>
        {tasks.map((task, index) => (
          <tr key={task.id}>
            <td>{index + 1}</td>
            <td>{task.projectName}</td>
            <td>{task.institutionName}</td>
            <td>{groupTypeLabel(task.groupType)}</td>
            <td>{task.groupCode}</td>
            <td>{task.reviewerName}</td>
            <td>{task.reviewerTitle}</td>
            <td>{task.reviewerInstitutionName}</td>
            <td>{statusLabel(task.status)}</td>
          </tr>
        ))}
      </tbody>
    </table>
  );
}

export default ReviewTaskList;
```

### 通用函数封装

```javascript
/**
 * 获取已分配任务列表
 * @param {number} competitionId - 赛事ID
 * @param {string} stage - 评审阶段（BOOK/INTERVIEW/FINAL）
 * @param {string} status - 任务状态（可选，PENDING/SCORED/RETURNED）
 * @param {string} token - 认证token
 * @returns {Promise<Array>} 任务列表
 */
async function getReviewTasks(competitionId, stage, status, token) {
  let url = `/api/admin/reviews/tasks?competitionId=${competitionId}&stage=${stage}`;
  
  if (status) {
    url += `&status=${status}`;
  }
  
  const response = await fetch(url, {
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });
  
  if (!response.ok) {
    throw new Error(`HTTP ${response.status}`);
  }
  
  const result = await response.json();
  
  if (!result.success) {
    throw new Error(result.message || '获取任务列表失败');
  }
  
  return result.data;
}

// 使用示例
const token = localStorage.getItem('token');

// 获取书审阶段所有任务
const bookTasks = await getReviewTasks(21, 'BOOK', null, token);

// 获取书审阶段待评审任务
const pendingTasks = await getReviewTasks(21, 'BOOK', 'PENDING', token);

// 获取面谈阶段已评分任务
const scoredTasks = await getReviewTasks(21, 'INTERVIEW', 'SCORED', token);
```

## 📊 数据示例

### 书审阶段任务

```json
{
  "id": 115,
  "stage": "BOOK",
  "status": "SCORED",
  "createdAt": "2026-02-06T18:12:37.447",
  "registrationId": 106,
  "projectName": "护理交接班规范化-1",
  "institutionName": "浙江大学医学院附属第二医院（浙二医院）",
  "groupType": "BASIC",
  "groupCode": "A1",
  "reviewerId": 6,
  "reviewerName": "李明华",
  "reviewerTitle": "Test Title",
  "reviewerInstitutionName": "浙江大学医学院附属第二医院（浙二医院）",
  "reviewerGroupCode": "A1",
  "interviewGroupCode": "A1",
  "expertBackground": "MEDICAL"
}
```

### 面谈阶段任务

```json
{
  "id": 116,
  "stage": "INTERVIEW",
  "status": "SCORED",
  "createdAt": "2026-02-06T18:13:15.123",
  "registrationId": 108,
  "projectName": "静脉输液安全改进-3",
  "institutionName": "杭州市中医院",
  "groupType": "ADVANCED",
  "groupCode": "B2",
  "reviewerId": 6,
  "reviewerName": "李明华",
  "reviewerTitle": "Test Title",
  "reviewerInstitutionName": "浙江大学医学院附属第二医院（浙二医院）",
  "reviewerGroupCode": "A1",
  "interviewGroupCode": "A1",
  "expertBackground": "MEDICAL"
}
```

## ✅ 修复总结

### 修改的文件

1. **新增文件**:
   - `src/main/java/com/trae/pinguan/web/dto/AdminReviewTaskItem.java` - 新的DTO

2. **修改文件**:
   - `src/main/java/com/trae/pinguan/service/ReviewService.java` - 添加 `listTasksForAdmin` 方法
   - `src/main/java/com/trae/pinguan/web/AdminReviewController.java` - 添加 `listTasks` 接口

3. **测试文件**:
   - `test_admin_review_tasks.py` - 测试脚本

### 测试结果

- ✅ 书审阶段任务查询正常
- ✅ 面谈阶段任务查询正常
- ✅ 状态筛选功能正常
- ✅ 所有字段都有值
- ✅ 字段完整性100%

### 前端需要修改

前端需要将原来的接口调用改为新接口：

**旧接口**（可能使用的）:
```javascript
// ❌ 这些接口不适合管理端使用
GET /api/reviews/tasks?reviewerId={id}
GET /api/reviews/tasks/stage?competitionId={id}&stage={stage}
```

**新接口**（应该使用的）:
```javascript
// ✅ 管理端专用接口
GET /api/admin/reviews/tasks?competitionId={id}&stage={stage}
GET /api/admin/reviews/tasks?competitionId={id}&stage={stage}&status={status}
```

## 🎯 下一步

1. **前端团队**：
   - 更新接口调用，使用新的 `/api/admin/reviews/tasks` 接口
   - 更新数据绑定，使用新的字段名
   - 测试验证所有字段都能正常显示

2. **后端团队**：
   - 待命支持
   - 如有其他字段需求，可以继续扩展 `AdminReviewTaskItem` DTO

3. **测试团队**：
   - 验证修复后的效果
   - 确认所有字段都能正常显示
   - 测试筛选功能

---

**修复时间**: 2026-02-08  
**修复人员**: 后端开发团队  
**测试状态**: ✅ 通过  
**前端状态**: ⏳ 待更新接口调用

