================================================================================
                    入围管理API废弃与替换快速对照
================================================================================

更新日期: 2026-02-07

================================================================================
  变更总览
================================================================================

新增API:
  ✨ GET /api/registrations/{id}/reviewer-scores?stage={BOOK|INTERVIEW|FINAL}
     功能: 返回指定项目每个评委的详细评分记录

废弃API:
  ❌ GET /api/admin/reviews/feedback
     原因: 新API完全覆盖并超越了此API的功能

================================================================================
  详细对比
================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│ ❌ 被废弃的API #1                                                        │
├─────────────────────────────────────────────────────────────────────────┤
│ API路径: GET /api/admin/reviews/feedback                               │
│ 参数: competitionId, stage                                              │
├─────────────────────────────────────────────────────────────────────────┤
│ 返回内容:                                                               │
│   - reviewerId, reviewerName                                            │
│   - total (总分) ✅                                                     │
│   - highlight, weakness ✅                                              │
├─────────────────────────────────────────────────────────────────────────┤
│ 不足:                                                                   │
│   ❌ 缺少分项评分（7个维度）                                            │
│   ❌ 缺少评委单位、职称                                                 │
│   ❌ 缺少评审时间                                                       │
│   ❌ 需要按赛事查询，返回所有项目，需前端筛选                           │
├─────────────────────────────────────────────────────────────────────────┤
│ 替换方案: GET /api/registrations/{id}/reviewer-scores                  │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ ✨ 新增API                                                               │
├─────────────────────────────────────────────────────────────────────────┤
│ API路径: GET /api/registrations/{registrationId}/reviewer-scores       │
│ 参数: stage (可选: BOOK|INTERVIEW|FINAL)                                │
├─────────────────────────────────────────────────────────────────────────┤
│ 返回内容:                                                               │
│   - reviewerId, reviewerName, reviewerTitle ✅                          │
│   - reviewerInstitutionName, reviewerInstitutionLevel ✅                │
│   - scores: { plan, problem, action, success, review,                  │
│               operation, presentation, total } ✅                       │
│   - highlight, weakness ✅                                              │
│   - submittedAt (评审时间) ✅                                           │
├─────────────────────────────────────────────────────────────────────────┤
│ 优势:                                                                   │
│   ✅ 包含7个维度的分项评分                                              │
│   ✅ 包含评委完整信息（姓名、职称、单位、等级）                         │
│   ✅ 包含评审时间                                                       │
│   ✅ 直接按项目ID查询，无需筛选                                         │
│   ✅ 支持按stage过滤                                                    │
│   ✅ 数据结构更清晰                                                     │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ ⚠️ 可选废弃的API #2                                                      │
├─────────────────────────────────────────────────────────────────────────┤
│ API路径: GET /api/registrations/{id}/review-details                    │
│ 参数: registrationId (路径参数)                                         │
├─────────────────────────────────────────────────────────────────────────┤
│ 返回内容:                                                               │
│   - avgPlan, avgProblem, avgAction... (平均分) ✅                       │
│   - avgTotal (平均总分) ✅                                              │
│   - highlights[], weaknesses[] (所有评委评语合并) ✅                    │
│   - taskCount, scoredCount ✅                                           │
├─────────────────────────────────────────────────────────────────────────┤
│ 优点:                                                                   │
│   ✅ 返回汇总平均分，适合快速查看                                       │
│   ✅ 数据量小，响应快                                                   │
├─────────────────────────────────────────────────────────────────────────┤
│ 不足:                                                                   │
│   ❌ 无法区分每个评委的评分                                             │
│   ❌ 无法区分评语是哪个评委的                                           │
├─────────────────────────────────────────────────────────────────────────┤
│ 建议: 根据前端需求决定是否保留                                         │
│   - 如果只展示每个评委详情 → 可废弃                                    │
│   - 如果需要快速查看平均分 → 建议保留                                  │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ ✅ 继续保留的API #3                                                      │
├─────────────────────────────────────────────────────────────────────────┤
│ API路径: GET /api/admin/reviews/rankings                               │
│ 参数: competitionId, stage, groupType                                  │
├─────────────────────────────────────────────────────────────────────────┤
│ 用途: 排行榜、列表页展示                                               │
│ 返回: 所有项目的排名和平均分                                           │
│ 状态: ✅ 保留（新API无法替代）                                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ ✅ 继续保留的API #4                                                      │
├─────────────────────────────────────────────────────────────────────────┤
│ API路径: GET /api/admin/reviews/shortlist                              │
│ 参数: competitionId, stage, limit, minAvgTotal                         │
├─────────────────────────────────────────────────────────────────────────┤
│ 用途: 入围名单筛选（按比例、按分数线）                                 │
│ 状态: ✅ 保留（新API无法替代）                                          │
└─────────────────────────────────────────────────────────────────────────┘

================================================================================
  API对比总结表
================================================================================

API                                      | 状态          | 原因
-----------------------------------------|---------------|---------------------
GET /api/admin/reviews/feedback          | ❌ 废弃       | 新API完全覆盖
GET /api/registrations/{id}/review-details | ⚠️ 可选废弃 | 根据前端需求决定
GET /api/admin/reviews/rankings          | ✅ 保留       | 用于列表页排名
GET /api/admin/reviews/shortlist         | ✅ 保留       | 用于入围筛选
GET /api/registrations/{id}/reviewer-scores | ✨ 新增    | 展示评委详情

================================================================================
  推荐方案
================================================================================

方案名称: 保守方案（API共存）⭐ 推荐

立即废弃:
  ❌ GET /api/admin/reviews/feedback

继续保留:
  ✅ GET /api/registrations/{id}/review-details (汇总平均分)
  ✅ GET /api/admin/reviews/rankings (排行榜)
  ✅ GET /api/admin/reviews/shortlist (入围筛选)

新增:
  ✨ GET /api/registrations/{id}/reviewer-scores (评委详细评分)

理由:
  1. 向下兼容，前端可渐进式迁移
  2. 每个API职责清晰
  3. /review-details 适合快速查看平均分（数据量小）
  4. 新API专注于展示每个评委的详细记录

================================================================================
  使用场景划分
================================================================================

入围管理列表页:
  └─ GET /api/admin/reviews/rankings
     返回: 所有项目的排名和平均分

项目详情弹窗 - 汇总信息:
  └─ GET /api/registrations/{id}/review-details
     返回: 各维度平均分、评语汇总

项目详情弹窗 - 评委详情:
  └─ GET /api/registrations/{id}/reviewer-scores
     返回: 每个评委的详细评分和信息

入围名单筛选:
  └─ GET /api/admin/reviews/shortlist
     返回: 符合条件的入围项目

================================================================================
  前端调用变更
================================================================================

❌ 旧的调用方式（被废弃）:

  // 查看项目的评委反馈
  const response = await axios.get('/api/admin/reviews/feedback', {
    params: { competitionId: 21, stage: 'BOOK' }
  });
  
  // 需要筛选出当前项目
  const projectFeedback = response.data.data.filter(
    item => item.registrationId === 106
  );


✅ 新的调用方式（推荐）:

  // 直接获取项目的评委详细评分
  const response = await axios.get('/api/registrations/106/reviewer-scores', {
    params: { stage: 'BOOK' }  // 可选
  });
  
  const reviewerScores = response.data.data;
  
  // 展示每个评委的详细记录
  reviewerScores.forEach(reviewer => {
    console.log(`评委: ${reviewer.reviewerName}`);
    console.log(`单位: ${reviewer.reviewerInstitutionName}`);
    console.log(`总分: ${reviewer.scores.total}`);
    console.log(`计划: ${reviewer.scores.plan}分`);
    console.log(`问题: ${reviewer.scores.problem}分`);
    console.log(`行动: ${reviewer.scores.action}分`);
    console.log(`成效: ${reviewer.scores.success}分`);
    console.log(`回顾: ${reviewer.scores.review}分`);
    console.log(`亮点: ${reviewer.highlight}`);
    console.log(`改进建议: ${reviewer.weakness}`);
    console.log(`评审时间: ${reviewer.submittedAt}`);
  });

================================================================================
  执行步骤
================================================================================

第一阶段: 新增API（不影响现有功能）
  1. ✅ 实现新API
  2. ✅ 编写测试脚本
  3. ✅ 提供前端文档

第二阶段: 前端迁移
  1. 前端新增"评委详情"功能，调用新API
  2. 保留原有"汇总信息"，继续使用 /review-details
  3. 移除对 /feedback 的调用（已废弃）

第三阶段: 清理废弃API
  1. 标记 /feedback 为 @Deprecated
  2. 观察确认无调用
  3. 移除废弃API代码

================================================================================
  总结
================================================================================

废弃API数量: 1个
  - GET /api/admin/reviews/feedback

替换方案:
  - GET /api/registrations/{id}/reviewer-scores

保留API数量: 3个
  - GET /api/registrations/{id}/review-details (汇总)
  - GET /api/admin/reviews/rankings (排行榜)
  - GET /api/admin/reviews/shortlist (入围筛选)

新增API数量: 1个
  - GET /api/registrations/{id}/reviewer-scores (评委详情)

前端影响: 小（渐进式迁移）

推荐方案: 保守方案（API共存）

================================================================================

详细文档: docs/入围管理API废弃与替换说明.md
