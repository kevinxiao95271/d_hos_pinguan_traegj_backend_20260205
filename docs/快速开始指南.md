# ⚡ 快速开始指南

## 🎯 一句话总结

**参赛者自己注册，评委管理员创建，管理员可查看和禁用所有用户。**

---

## 👥 用户类型

| 用户 | 怎么注册 | 怎么登录 | 谁管理 |
|------|---------|---------|--------|
| **参赛者** | 自己注册 | 手机号+密码 | 管理员可禁用 |
| **评委** | 管理员创建 | 手机号+初始密码 | 管理员创建 |
| **管理员** | 系统初始化 | 手机号+密码 | 超级管理员 |

---

## 🚀 参赛者注册（3步）

### 步骤1: 选择机构

调用接口搜索36K+医疗机构：

```javascript
POST /api/institutions/search
{
  "keyword": "人民医院",
  "region": "杭州市",
  "page": 0,
  "size": 20
}
```

### 步骤2: 填写注册信息

```javascript
POST /api/auth/register
{
  "phone": "13800138000",        // 手机号
  "password": "password123",     // 密码（6-20位）
  "confirmPassword": "password123",
  "name": "张三",                // 姓名
  "title": "主任医师",           // 职称（可选）
  "role": "CONTESTANT",          // 固定为参赛者
  "institutionId": 123           // 机构ID
}
```

### 步骤3: 注册成功，自动登录

返回Token，保存到localStorage，进入系统。

**⚠️ 无需等待审核，立即可用！**

---

## 👨‍⚕️ 评委账号创建（管理员操作）

### 管理员创建评委

```javascript
POST /api/admin/users/reviewers
Authorization: Bearer {管理员token}

{
  "phone": "13900139000",
  "name": "李教授",
  "title": "主任医师",
  "institutionId": 456,
  "reviewerGroupCode": "GROUP_A",
  "expertBackground": "心内科"
}

响应:
{
  "userId": 10,
  "phone": "13900139000",
  "name": "李教授",
  "initialPassword": "654321",  // ⚠️ 系统分配的初始密码
  "institutionName": "某医院"
}
```

### 通知评委

**必须告诉评委**:
- 手机号：13900139000
- 初始密码：654321
- 登录地址：xxx
- 提醒：首次登录后尽快修改密码

---

## 🔐 登录（所有用户）

```javascript
POST /api/auth/login-with-password
{
  "phone": "13800138000",
  "password": "password123"
}

响应:
{
  "id": 1,
  "phone": "13800138000",
  "name": "张三",
  "role": "CONTESTANT",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  ...
}
```

**验证逻辑**:
1. ✅ 手机号和密码正确
2. ✅ 账号未被禁用
3. ✅ 更新最后登录时间

---

## 🛠️ 管理员功能

### 查看所有用户

```javascript
POST /api/admin/users/query
Authorization: Bearer {token}

{
  "phone": "138",          // 可选：手机号搜索
  "name": "张",            // 可选：姓名搜索
  "role": "CONTESTANT",    // 可选：角色筛选
  "enabled": true,         // 可选：状态筛选
  "page": 0,
  "size": 20
}
```

### 禁用用户

```javascript
PUT /api/admin/users/{userId}/disable
Authorization: Bearer {token}

// 用户立即无法登录
```

### 启用用户

```javascript
PUT /api/admin/users/{userId}/enable
Authorization: Bearer {token}

// 恢复正常使用
```

### 用户统计

```javascript
GET /api/admin/users/statistics
Authorization: Bearer {token}

响应:
{
  "totalUsers": 200,
  "enabledUsers": 195,
  "disabledUsers": 5,
  "contestants": 150,
  "reviewers": 45,
  ...
}
```

---

## 📊 完整流程图

```
┌─────────────────────────────────────────┐
│           用户角色分流                   │
└─────────────────────────────────────────┘
          ↓                    ↓
    【参赛者】              【评委】
          ↓                    ↓
  ┌──────────────┐    ┌──────────────┐
  │ 1.选择机构   │    │ 管理员创建   │
  │ 2.填写信息   │    │ 系统分配密码 │
  │ 3.设置密码   │    │ 通知评委     │
  │ 4.提交注册   │    └──────────────┘
  └──────────────┘             ↓
          ↓                    ↓
  ┌──────────────┐    ┌──────────────┐
  │ 注册成功     │    │ 首次登录     │
  │ enabled=true │    │ 修改初始密码 │
  │ 自动登录     │    └──────────────┘
  └──────────────┘             ↓
          ↓                    ↓
    ┌─────────────────────────────┐
    │      进入系统使用            │
    └─────────────────────────────┘
                 ↓
       ┌──────────────────┐
       │   管理员监控      │
       │ - 查看用户列表    │
       │ - 禁用违规账号    │
       │ - 启用正常账号    │
       │ - 创建评委账号    │
       └──────────────────┘
```

---

## 🔑 关键接口速查

### 参赛者

| 操作 | 接口 | 说明 |
|------|------|------|
| 搜索机构 | `POST /api/institutions/search` | 选择所属机构 |
| 注册 | `POST /api/auth/register` | 自助注册 |
| 登录 | `POST /api/auth/login-with-password` | 密码登录 |
| 改密码 | `POST /api/auth/change-password/{id}` | 修改密码 |

### 管理员

| 操作 | 接口 | 说明 |
|------|------|------|
| 查用户 | `POST /api/admin/users/query` | 查询列表 |
| 建评委 | `POST /api/admin/users/reviewers` | 创建评委 |
| 禁用 | `PUT /api/admin/users/{id}/disable` | 禁用账号 |
| 启用 | `PUT /api/admin/users/{id}/enable` | 启用账号 |
| 统计 | `GET /api/admin/users/statistics` | 用户统计 |

---

## 🧪 快速测试

```bash
# 1. 启动服务
mvn spring-boot:run

# 2. 访问Swagger
http://localhost:6031/swagger-ui.html

# 3. 运行测试
python scripts/test_user_management.py
```

---

## ⚠️ 重要提示

### 1. 评委不能自己注册

如果评委尝试注册，会收到错误：
```
"仅允许参赛者自助注册，评委请联系管理员创建账号"
```

### 2. 初始密码保管

创建评委后，系统返回初始密码（6位数字），**必须立即记录并通知评委**。

### 3. 禁用功能

禁用后用户无法登录，但已登录的Token仍然有效（直到过期）。

---

## 📞 帮助

- **Swagger**: http://localhost:6031/swagger-ui.html
- **完整文档**: `完整功能交付总结.md`
- **用户管理**: `README_用户管理.md`
- **机构选择**: `docs/机构选择功能使用手册.md`

---

**状态**: ✅ 已完成，可用  
**版本**: 1.0  
**日期**: 2026-02-25
