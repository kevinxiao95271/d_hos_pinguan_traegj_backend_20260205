# 历史材料文件重复记录清理报告

**生成时间：** 2026-02-28  
**分析人员：** System  
**状态：** ⚠️ 发现重复记录，需要清理

---

## 📊 问题发现

### 1. 重复记录统计

通过数据库分析，发现以下重复记录：

| 报名ID | 文件类型 | 文件数量 | 最早上传时间 | 最新上传时间 |
|--------|----------|----------|--------------|--------------|
| 131 | REGISTRATION_FORM | 5 | 2026-02-28 18:08:05 | 2026-02-28 18:10:46 |

**总结：**
- 发现 **1组** 重复记录
- 需要删除 **4个** 旧文件
- 只保留 **1个** 最新文件

---

### 2. 详细记录信息

#### 报名ID: 131 - REGISTRATION_FORM类型

| ID | 文件名 | 上传时间 | 操作 |
|----|--------|----------|------|
| 5 | 品管大赛报名表-9911.docx | 2026-02-28 18:10:46 | ✅ **保留（最新）** |
| 4 | test.docx | 2026-02-28 18:08:06 | ❌ 删除（旧文件） |
| 3 | 品管大赛报名表-9911.docx | 2026-02-28 18:08:06 | ❌ 删除（旧文件） |
| 2 | test.docx | 2026-02-28 18:08:05 | ❌ 删除（旧文件） |
| 1 | test.docx | 2026-02-28 18:08:05 | ❌ 删除（旧文件） |

**将要删除的记录ID:** 1, 2, 3, 4  
**保留的记录ID:** 5

---

## 🔧 清理方案

### 方案1: 使用SQL脚本（推荐）

**脚本文件:** `scripts/cleanup_duplicates_execute.sql`

**执行步骤:**

1. **备份数据（重要！）**
   ```sql
   CREATE TABLE material_files_backup_20260228 AS 
   SELECT * FROM material_files;
   ```

2. **删除重复记录**
   ```sql
   DELETE FROM material_files
   WHERE id IN (1, 2, 3, 4);
   -- 或使用脚本中的复杂查询自动识别
   ```

3. **验证结果**
   ```sql
   SELECT registration_id, type, COUNT(*) as file_count
   FROM material_files
   GROUP BY registration_id, type
   HAVING COUNT(*) > 1;
   -- 应该返回空结果
   ```

### 方案2: 使用Python脚本

**脚本文件:** `scripts/cleanup_duplicates_simple.py`

**执行命令:**
```bash
python scripts/cleanup_duplicates_simple.py
```

脚本会：
- ✅ 显示将要删除的文件
- ✅ 要求用户确认
- ✅ 删除数据库记录
- ✅ 可选删除MinIO中的物理文件

---

## 📁 MinIO文件清理

### 需要删除的MinIO文件路径:

根据数据库记录，需要删除以下MinIO文件：

| ID | file_url |
|----|----------|
| 1 | 131/{uuid}-test.docx |
| 2 | 131/{uuid}-test.docx |
| 3 | 131/{uuid}-品管大赛报名表-9911.docx |
| 4 | 131/{uuid}-test.docx |

**注意:** 具体的UUID路径需要从数据库的 `file_url` 字段获取

---

## ✅ 清理后的预期结果

### 清理前:
```
报名ID: 131
  - REGISTRATION_FORM: 5个文件（混乱）
```

### 清理后:
```
报名ID: 131
  - REGISTRATION_FORM: 1个文件（最新的）
```

---

## 🚀 执行建议

### 推荐执行顺序:

1. **✅ 已完成** - 代码修复（MaterialService.java）
   - 已添加自动删除旧文件的逻辑
   - 新上传的文件会自动清理旧的

2. **📋 待执行** - 清理历史重复记录
   ```sql
   -- 连接数据库
   mysql -h gz-cdb-bq7gk3k5.sql.tencentcdb.com -P 63606 -u root -p d_hos_pinguan_traegj_20260205
   
   -- 执行清理脚本
   source scripts/cleanup_duplicates_execute.sql;
   ```

3. **🧹 可选** - 清理MinIO物理文件
   - 数据库记录删除后，物理文件仍占用空间
   - 可通过Python脚本或MinIO控制台手动删除

---

## ⚠️ 注意事项

1. **必须先备份！**
   - 清理前务必创建备份表
   - 建议: `material_files_backup_20260228`

2. **MinIO文件**
   - 删除数据库记录不会自动删除MinIO文件
   - 需要手动清理或使用Python脚本

3. **新上传不会有问题**
   - 代码已修复，新上传会自动删除旧文件
   - 此次清理是一次性的历史数据清理

4. **影响范围**
   - 仅影响报名ID 131的记录
   - 其他报名不受影响

---

## 📝 执行记录

### 待填写（执行后更新）:

- [ ] 备份创建时间: __________
- [ ] 清理执行时间: __________
- [ ] 删除记录数量: __________
- [ ] MinIO文件删除: 是 / 否
- [ ] 验证通过: 是 / 否
- [ ] 执行人: __________

---

## 🔍 验证查询

### 验证是否还有重复记录:

```sql
SELECT 
    registration_id,
    type,
    COUNT(*) as file_count
FROM material_files
GROUP BY registration_id, type
HAVING COUNT(*) > 1;
```

**期望结果:** 无记录返回

### 查看报名131的当前文件:

```sql
SELECT 
    id,
    registration_id,
    type,
    file_name,
    uploaded_at
FROM material_files
WHERE registration_id = 131
ORDER BY type, uploaded_at DESC;
```

**期望结果:** 每个type只有一条记录

---

## 📞 联系方式

如有问题，请联系系统管理员。

---

**报告结束**
