# 云服务器部署完成报告

## 部署时间
2026-02-13 17:07

## 服务器信息
- **IP地址**: 81.71.44.180
- **用户**: root
- **部署目录**: /data
- **服务端口**: 6031

## 部署状态

### ✅ 部署成功

**服务状态**:
- ✅ jar文件已上传（47MB）
- ✅ 旧进程已停止
- ✅ 新服务已启动
- ✅ 端口6031已监听
- ✅ 服务正常运行

**进程信息**:
- PID: 889554
- 启动时间: 17:06
- 启动耗时: 9.39秒

## 访问地址

### API服务
```
http://81.71.44.180:6031
```

### Swagger文档
```
http://81.71.44.180:6031/swagger-ui/index.html
```

## 部署内容

### 本次更新功能

1. **面谈得分列表API**
   - 新增接口: `GET /api/admin/reviews/interview-scores`
   - 数据结构与书审一致

2. **评审专家列表增强**
   - 新增字段: `institutionLevel`（机构等级）
   - 接口: `GET /api/admin/reviewers`

3. **评审驳回再次打分**
   - 验证通过，支持驳回后重新打分

### Git版本
- **分支**: feat-20260212
- **最新commit**: d02f9a6
- **提交数**: 7个新commits

## 部署文件

### jar文件
- **文件名**: pinguan-backend-0.0.1-SNAPSHOT.jar
- **大小**: 47MB
- **位置**: /data/pinguan-backend-0.0.1-SNAPSHOT.jar

### 日志文件
- **位置**: /data/app.log
- **查看命令**: `tail -f /data/app.log`

## 服务管理命令

### 查看服务状态
```bash
ssh root@81.71.44.180 "ps aux | grep pinguan-backend | grep -v grep"
```

### 查看端口状态
```bash
ssh root@81.71.44.180 "netstat -tlnp | grep 6031"
```

### 查看日志
```bash
ssh root@81.71.44.180 "tail -f /data/app.log"
```

### 停止服务
```bash
ssh root@81.71.44.180 "pkill -f 'pinguan-backend-0.0.1-SNAPSHOT.jar'"
```

### 启动服务
```bash
ssh root@81.71.44.180 "cd /data && nohup java -jar pinguan-backend-0.0.1-SNAPSHOT.jar --server.port=6031 > app.log 2>&1 &"
```

### 重启服务
```bash
# 使用本地脚本
python scripts/restart_cloud_service.py

# 或手动执行
ssh root@81.71.44.180 "pkill -f 'pinguan-backend' && cd /data && nohup java -jar pinguan-backend-0.0.1-SNAPSHOT.jar --server.port=6031 > app.log 2>&1 &"
```

## 部署脚本

### 完整部署（上传jar + 重启）
```bash
python scripts/deploy_to_cloud_server.py
```

### 仅重启服务
```bash
python scripts/restart_cloud_service.py
```

### 检查服务状态
```bash
python scripts/check_cloud_service.py
```

## 启动日志摘要

```
2026-02-13 17:06:53.488  INFO 889554 --- [main] c.t.pinguan.PinguanBackendApplication
  : Starting PinguanBackendApplication

2026-02-13 17:06:57.580  INFO 889554 --- [main] com.zaxxer.hikari.HikariDataSource
  : HikariPool-1 - Start completed.

2026-02-13 17:07:02.805  INFO 889554 --- [main] o.s.b.w.embedded.tomcat.TomcatWebServer
  : Tomcat started on port(s): 6031 (http)

2026-02-13 17:07:02.827  INFO 889554 --- [main] c.t.pinguan.PinguanBackendApplication
  : Started PinguanBackendApplication in 9.39 seconds
```

## 数据库配置

服务使用远程PostgreSQL数据库：
- **主机**: 119.167.165.27:5432
- **数据库**: d_hos_pinguan_20260211
- **连接池**: HikariCP

## API测试

### 测试登录
```bash
curl -X POST http://81.71.44.180:6031/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "13800000041",
    "name": "CommitteeAdmin A",
    "role": "COMMITTEE_ADMIN"
  }'
```

### 测试评审专家列表
```bash
curl -X GET "http://81.71.44.180:6031/api/admin/reviewers" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 测试面谈得分列表
```bash
curl -X GET "http://81.71.44.180:6031/api/admin/reviews/interview-scores?competitionId=21" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 监控建议

### 1. 服务健康检查
定期检查服务是否运行：
```bash
*/5 * * * * curl -s http://81.71.44.180:6031/actuator/health || echo "Service down"
```

### 2. 日志监控
监控错误日志：
```bash
tail -f /data/app.log | grep -i error
```

### 3. 端口监控
确保端口6031始终监听：
```bash
netstat -tlnp | grep 6031
```

## 故障排查

### 服务无法启动
1. 检查端口是否被占用：`netstat -tlnp | grep 6031`
2. 查看启动日志：`tail -100 /data/app.log`
3. 检查jar文件是否存在：`ls -lh /data/pinguan-backend-0.0.1-SNAPSHOT.jar`

### 服务启动后立即停止
1. 查看完整日志：`cat /data/app.log`
2. 检查数据库连接：确认数据库服务正常
3. 检查配置文件：确认application.yml配置正确

### 无法访问API
1. 检查防火墙：确保端口6031开放
2. 检查服务状态：`ps aux | grep pinguan-backend`
3. 测试本地访问：`curl http://localhost:6031/actuator/health`

## 安全建议

1. **定期更新**: 及时更新jar文件到最新版本
2. **日志轮转**: 配置日志轮转避免磁盘占满
3. **备份策略**: 定期备份jar文件和配置
4. **监控告警**: 配置服务监控和告警机制

## 后续优化

### 1. 使用systemd管理服务
创建服务文件 `/etc/systemd/system/pinguan-backend.service`:
```ini
[Unit]
Description=Pinguan Backend Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/data
ExecStart=/usr/bin/java -jar /data/pinguan-backend-0.0.1-SNAPSHOT.jar --server.port=6031
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

然后使用systemd命令管理：
```bash
systemctl start pinguan-backend
systemctl stop pinguan-backend
systemctl restart pinguan-backend
systemctl status pinguan-backend
```

### 2. 配置Nginx反向代理
```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        proxy_pass http://localhost:6031;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 3. 配置日志轮转
创建 `/etc/logrotate.d/pinguan-backend`:
```
/data/app.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
}
```

## 总结

✅ **云服务器部署成功**

**部署信息**:
- 服务器: 81.71.44.180:6031
- 部署时间: 2026-02-13 17:07
- 服务状态: 运行正常
- 功能更新: 3个新功能

**访问地址**:
- API: http://81.71.44.180:6031
- Swagger: http://81.71.44.180:6031/swagger-ui/index.html

**前端可以开始对接云服务器API！**
